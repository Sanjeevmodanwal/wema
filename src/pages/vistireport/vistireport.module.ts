import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { VistireportPage } from './vistireport';

@NgModule({
  declarations: [
    VistireportPage,
  ],
  imports: [
    IonicPageModule.forChild(VistireportPage),
  ],
})
export class VistireportPageModule {}
